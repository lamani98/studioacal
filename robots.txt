User-agent: *
Allow: /

Sitemap: https://www.studioacal.com.br/sitemap.xml


.hero {
  position: relative;
  height: 100svh;
  display: flex;
  align-items: flex-end;
  overflow: hidden;
}

.hero .tone {
  position: absolute;
  inset: 0;
}

.hero::after {
  content: "";
  position: absolute;
  inset: 0;
  background-image: url("../img/capa2.png");
}

.hero__content {
  position: relative;
  z-index: 2;
  padding: var(--side);
  padding-bottom: clamp(48px, 8vw, 96px);
  color: var(--paper);
}